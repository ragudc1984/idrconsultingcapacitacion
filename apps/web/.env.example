# Copia este archivo como .env.local en apps/web. Vite lo lee al arrancar y al
# construir. No es secreto: la URL termina escrita en el JavaScript servido.

# Direccion del API de tareas. En desarrollo, el API local (npm run dev -w @idr/api).
VITE_API_URL=http://localhost:3000
