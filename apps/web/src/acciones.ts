/** Que accion esta guardandose sobre cada tarea mientras llega la respuesta. */
export type Accion = 'crear' | 'completar' | 'editar' | 'eliminar'

/** Clave del formulario de alta, que todavia no tiene id de tarea. */
export const NUEVA = 'nueva'
